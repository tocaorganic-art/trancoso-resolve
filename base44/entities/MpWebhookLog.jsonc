{
  "name": "MpWebhookLog",
  "type": "object",
  "description": "Registro idempotente de notificações recebidas do Mercado Pago. Cada notification_id é gravado antes de processar — se já existir, o evento é ignorado.",
  "properties": {
    "notification_id": {
      "type": "string",
      "description": "ID único da notificação MP (campo 'id' do payload). Chave de idempotência."
    },
    "topic": {
      "type": "string",
      "description": "Tipo do evento MP: payment, subscription_preapproval, subscription_authorized_payment"
    },
    "resource_id": {
      "type": "string",
      "description": "ID do recurso MP (payment.id, preapproval.id, etc)"
    },
    "status": {
      "type": "string",
      "enum": ["processado", "duplicado", "ignorado", "erro"],
      "description": "Resultado do processamento"
    },
    "payload_snapshot": {
      "type": "object",
      "description": "Snapshot sanitizado do payload para auditoria/reconciliação (sem PII nem dados financeiros)"
    },
    "erro": {
      "type": "string",
      "description": "Mensagem de erro se status=erro"
    },
    "recebido_em": {
      "type": "string",
      "format": "date-time",
      "description": "Data e hora em que a notificação foi recebida"
    }
  },
  "required": ["notification_id", "topic"],
  "rls": {
    "read": {
      "user_condition": { "role": "admin" }
    },
    "create": false,
    "update": false,
    "delete": false
  }
}
